/**
 * Implements hook_block_info().
 */
function sophist_block_info() {
  $blocks = array();
  $blocks['info'] = array(
	'info' => t('Sophist Zone');
  );
  return $blocks;
}

/**
 * Implements hook_block_view().
 */
function sophist_block_view($delta = '') {
	$block = array();
	$block['subject'] = t('Forecast');
	$block['content'] = 'First block of Sophist';
	return $block;
}